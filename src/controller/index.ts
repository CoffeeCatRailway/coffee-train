import {Actions, Controller, IPluginController} from '@rrox/api';

export default class CoffeeTrainPlugin extends Controller {

    public async load(controller: IPluginController) {
    }

    public async unload(controller: IPluginController) {

    }
}